using UnityEngine;
using System.Collections;

public class HiddenMessage : MonoBehaviour {

	// Use this for initialization
	void Start () {
		print("You found the message, you must be the hero destined to save the minions from their destruction");
		print ("Gru is going to go crazy as there is no bananas left, help them!");
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
